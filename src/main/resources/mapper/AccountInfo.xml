<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="cn.edu.uestc.jianhang.dao.LoginDao">

	<select id="selectByAccountInfo" parameterType="AccountInfo" resultType="AccountInfo">
		select * from employee where account_id
	</select>
	

</mapper>